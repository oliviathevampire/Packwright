package net.vampirestudios.packwright.data.loot.functions;

import net.vampirestudios.packwright.data.loot.LootFunction;

/**
 * Escape hatch for modded functions or vanilla fields Packwright has not wrapped yet.
 */
public class CustomLootFunction extends LootFunction {
	public CustomLootFunction(String function) {
		super(function);
	}
}
